[project]
name = "aws-hackathon-content-creator-copilot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "packages>=0.1.1",
]
